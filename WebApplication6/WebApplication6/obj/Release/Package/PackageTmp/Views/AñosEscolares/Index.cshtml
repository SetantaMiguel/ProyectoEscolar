@model IEnumerable<WebApplication6.Models.AñosEscolares>

@{
    Layout = null;
}


    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AñoEscolar)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AñoEscolar)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id_AñoEscolares }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id_AñoEscolares }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id_AñoEscolares })
            </td>
        </tr>
    }
    
    </table>

